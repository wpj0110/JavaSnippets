public interface Design {
    void FillColor();
    int add(int a, int b);

    //int giveNum(){ //Everything within an interface is abstract therefore it can't have a body.
    //    return 1;
    //}
}
